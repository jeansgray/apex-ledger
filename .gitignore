# Local runtime — personal financial data (never commit)
.env
.env.*
!.env.example
data/
!src/apex_ledger/data/
*.db
skills/manifest.candidates.json

# Explicit: Plaid tokens, ledger, local integrations live under data/
# data/integrations.json  — Plaid/Schwab OAuth tokens
# data/ledger.db          — holdings, transactions, reconciliations

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/

# Local secrets (never commit)
.config/
*.local.env

# Note: real PAT is in ~/.config/apex-ledger/env (outside repo)
